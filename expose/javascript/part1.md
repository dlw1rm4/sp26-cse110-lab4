# Expose: Part 1
## 1. What is printed by line 9? If the code returns an error, explain why. 
The code prints out 'values added: 20' by line 9.

## 2. What is printed by line 13? If the code returns an error, explain why. 
The code prints out 'final result: 20' by line 13.

## 3. Why should you not use var? Explain why. 
You shouldn't use var because it can lead to naming conflicts and scoping issues.

## 4. What is printed by line 9? If the code returns an error, explain why. 
The code prints out 'values added: 20' by line 9.

## 5. What is printed by line 13? If the code returns an error, explain why. 
The code returns an error because the keyword 'let' makes the variable that follows it only exist within the block's scope. Therefore, it cannot exist outside of it, which includes line 13.

## 6. What is printed by line 9? If the code returns an error, explain why. 
The code returns an error because the keyword const makes it so that the variable that follows it cannot be changed.

## 7. What is printed by line 13? If the code returns an error, explain why. 
The code returns an error because the keyword const makes it so that the variable that follows it cannot be changed. However, the keyword const also only exists in the block's scope as well, so it cannot exist in line 13.