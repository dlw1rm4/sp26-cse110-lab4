# Explore: Basic DevTools
## 1. What was the bug?
The bug is that both of the inputs are strings. So, when you add both the strings, it concatenates it rather than adds their integers.
## 2. How would you fix it? Include a screenshot of your fix. Name it fix.png (or whatever image extension you would like to use) and add it to your expand/screenshots directory.
I would fix it by typecasting num1 and num2 as a number.